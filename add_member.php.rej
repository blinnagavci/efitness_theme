diff a/add_member.php b/add_member.php	(rejected hunks)
@@ -595,7 +595,7 @@
                     "showMethod": "fadeIn",
                     "hideMethod": "fadeOut"
                 };
-                toastr.error("Unfortunately, we ran into some problems trying to add the member", opts);
+                toastr.error("Unfortunately, we ran into some problems trying to add the member.", opts);
             }
         </script>
 
